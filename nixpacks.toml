# Nixpacks build configuration for educado-api
# Substitui o Dockerfile — Coolify detecta automaticamente

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node build/index.js"
